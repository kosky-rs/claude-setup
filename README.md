# Claude Code Complete Setup Package

Claude Codeの開発環境を最適化するための包括的なセットアップパッケージです。

## 📦 パッケージ内容

```
claude-code-setup/
├── Claude_Code_Complete_Setup_Guide.md   # 完全ガイド（仕様書兼マニュアル）
├── setup-global.sh                        # グローバル設定セットアップスクリプト
├── setup-project.sh                       # プロジェクト設定セットアップスクリプト
├── README.md                              # このファイル
└── templates/
    ├── global/                            # グローバル設定テンプレート
    │   ├── CLAUDE.md
    │   ├── settings.json
    │   ├── claude.json                    # → ~/.claude.json
    │   ├── issues.json                    # 🆕 グローバルIssue管理
    │   ├── upgrade-log.md                 # 🆕 アップグレード履歴
    │   ├── docs/
    │   │   ├── behavior-principles.md
    │   │   ├── task-workflow.md
    │   │   ├── quality-gates.md
    │   │   └── code-standards.md
    │   └── commands/
    │       ├── checkpoint.md
    │       ├── review.md
    │       ├── upgrade-global.md          # 🆕 設定アップグレード
    │       └── verify-upgrade.md          # 🆕 効果検証
    └── project/                           # プロジェクト設定テンプレート
        ├── CLAUDE.md
        ├── .mcp.json
        ├── tasks.json
        ├── progress.md
        ├── issues.json                    # 🆕 プロジェクトIssue管理
        ├── .env.mcp.example
        ├── .gitignore.claude
        ├── .claude/
        │   ├── settings.json
        │   ├── commands/
        │   │   ├── start-task.md
        │   │   ├── complete-task.md
        │   │   ├── log-issue.md           # 🆕 Issue記録
        │   │   ├── retrospective.md       # 🆕 セッション振り返り
        │   │   └── sync-issues-to-global.md  # 🆕 グローバル同期
        │   └── agents/
        │       └── security-reviewer.md
        └── docs/
            ├── architecture.md
            └── api-specs.md
```

## 🚀 クイックスタート

### Step 1: グローバル設定のセットアップ

```bash
# パッケージを展開
unzip claude-code-setup.zip
cd claude-code-setup

# グローバルセットアップを実行
./setup-global.sh
```

これにより以下が設定されます：
- `~/.claude/CLAUDE.md` - グローバル指示
- `~/.claude/settings.json` - Hooks、Permissions
- `~/.claude.json` - User scope MCP（Context7、Sequential Thinking）
- `~/.claude/issues.json` - グローバルIssue管理
- `~/.claude/docs/` - 詳細ドキュメント
- `~/.claude/commands/` - グローバルコマンド

### Step 2: プロジェクト設定のセットアップ

```bash
# プロジェクトディレクトリに移動
cd /path/to/your/project

# プロジェクトセットアップを実行
/path/to/claude-code-setup/setup-project.sh
```

これにより以下が設定されます：
- `CLAUDE.md` - プロジェクト固有指示
- `.mcp.json` - Project scope MCP
- `tasks.json` - タスク管理
- `issues.json` - プロジェクトIssue管理
- `progress.md` - 進捗ログ
- `.claude/` - コマンド、エージェント、設定

### Step 3: 環境変数の設定

```bash
# MCP用環境変数をコピー
cp .env.mcp.example .env.mcp

# 編集してAPIキー等を設定
vim .env.mcp

# シェルに読み込み（~/.bashrcに追加推奨）
source .env.mcp
```

### Step 4: 動作確認

```bash
# Claude Codeを起動
claude

# コンテキスト確認
/context

# MCP接続確認
/mcp

# カスタムコマンド確認
/help
```

## 📖 主な機能

### 1. Progressive Disclosure設計
- CLAUDE.mdはモジュラー構成で@importを活用
- 必要な時だけ詳細をロード
- トークン消費を最小化

### 2. タスク駆動開発ワークフロー
- `tasks.json` でタスク状態管理
- `progress.md` で進捗記録
- セッションライフサイクルの標準化

### 3. MCP最適化
- User scope: 常用MCP（Context7等）
- Project scope: プロジェクト固有MCP
- トークンコストを意識した構成

### 4. 品質ゲート
- テスト必須
- コミット必須
- コンテキスト監視

### 5. 🆕 Issue管理 & 自動育成システム

プロジェクト横断でIssueを蓄積し、CLAUDE.mdを継続的に改善：

```
セッション中 → /log-issue で問題記録
    ↓
セッション終了 → /retrospective で振り返り
    ↓
プロジェクト終了 → /sync-issues-to-global で蓄積
    ↓
定期メンテナンス → /upgrade-global で設定改善
    ↓
効果検証 → /verify-upgrade で確認
```

## 🔧 カスタムコマンド一覧

### グローバルコマンド（~/.claude/commands/）

| コマンド | 説明 |
|---------|------|
| `/checkpoint` | セッション保存・引き継ぎ |
| `/review` | コードレビュー |
| `/upgrade-global` | 🆕 蓄積Issueを分析し設定をアップグレード |
| `/verify-upgrade` | 🆕 アップグレードの効果を検証 |

### プロジェクトコマンド（.claude/commands/）

| コマンド | 説明 |
|---------|------|
| `/start-task [N]` | タスク開始・計画作成 |
| `/complete-task [N]` | タスク完了・検証 |
| `/log-issue [desc]` | 🆕 問題発生時にIssue記録 |
| `/retrospective` | 🆕 セッション終了時の振り返り |
| `/sync-issues-to-global` | 🆕 プロジェクトIssueをグローバルに同期 |

## 🔄 育成ワークフロー

### 日常の開発サイクル

```
1. セッション開始
   └─ /start-task で作業開始

2. 作業中
   └─ 問題発生時 → /log-issue で即座に記録

3. セッション終了
   └─ /retrospective で振り返り・未検出Issue発見

4. プロジェクト終了
   └─ /sync-issues-to-global でグローバルに蓄積
```

### 定期メンテナンス（週次推奨）

```
1. /upgrade-global
   └─ 蓄積したIssueを分析
   └─ CLAUDE.md/settings.jsonの改善を提案
   └─ 人間が承認 → 適用

2. 5セッション後
   └─ /verify-upgrade で効果検証
   └─ 効果なし → 再度アップグレード検討
```

## 📚 詳細ドキュメント

完全な仕様と詳細は `Claude_Code_Complete_Setup_Guide.md` を参照してください。

## 🔧 カスタマイズ

### CLAUDE.mdのカスタマイズ

`templates/global/CLAUDE.md` と `templates/project/CLAUDE.md` を編集して、自分のワークフローに合わせてカスタマイズできます。

### MCPの追加

```bash
# User scopeに追加
claude mcp add <n> -s user -- <command>

# Project scopeに追加
claude mcp add <n> -s project -- <command>
```

### カスタムコマンドの追加

`.claude/commands/` に新しい `.md` ファイルを作成：

```markdown
---
description: Your command description
---

# Command Title

[Instructions]
```

## ⚠️ 注意事項

- Node.js 18+ が必要です
- 既存ファイルは上書き確認されます
- `.env.mcp` は必ず `.gitignore` に追加してください
- `/upgrade-global` でのCLAUDE.md変更は**必ず人間の承認後**に適用されます

## 📝 ライセンス

このパッケージは自由に使用・改変できます。

---

Created by Kosky | Last Updated: 2025-12-01
